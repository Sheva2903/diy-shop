# Copy to .env.local. Only the publishable key belongs here — it ships in the
# browser bundle. The secret key must never appear in frontend code.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxx
